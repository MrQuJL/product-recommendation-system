在使用springMVC的时候没有办法通过相对路径将图片写入项目中，只能使用绝对路径来写入文件。
但是在读取的时候，却不能使用绝对路径来访问刚才上传的图片（即使使用了file:\\\协议也不行）。

原因：服务端不能主动的去访问客户端的资源，如果能访问那不就成了黑客行为了？

解决：再新建一个项目专门用来保存图片，原项目中通过绝对路径写入新项目的根目录，读取的时候通过新项目的uri来获取资源。perfect!

